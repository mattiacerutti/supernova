{
  "name": "@supernova/server",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "exports": {
    "./process": "./src/process.ts"
  },
  "bin": {
    "supernova-server": "./dist/cli.js"
  },
  "scripts": {
    "dev": "SUPERNOVA_SERVER_DEV=1 bun --watch src/cli.ts",
    "start": "node dist/cli.js",
    "build": "rm -rf dist && bun build src/cli.ts --define PI_BUNDLED_NODE=true --target node --outdir dist && bun scripts/prepare-tools.ts",
    "test": "bun test tests",
    "build:e2e": "bun run build && bun build tests/support/e2e-server.ts --define PI_BUNDLED_NODE=true --target node --outfile dist/e2e-server.js",
    "lint": "eslint .",
    "prettier": "prettier --check . --ignore-path ../../.prettierignore",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@effect/platform-node": "4.0.0-beta.60",
    "@supernova/agent-runtime": "workspace:*",
    "@supernova/contracts": "workspace:*",
    "commander": "^14.0.2",
    "effect": "4.0.0-beta.60"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@earendil-works/pi-ai": "0.85.1",
    "@types/node": "^22.19.1",
    "eslint": "^10.3.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.59.1"
  }
}
